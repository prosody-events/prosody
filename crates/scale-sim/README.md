# Prosody Scale Simulator

This crate contains an independent discrete-event plant for the autoscaling
core. The plant does not reuse controller equations.

Tests drive virtual time and can write deterministic SVG evidence. Plots aid
review, but assertions determine success.
